@model IEnumerable<HRSystem.Models.Leave>

@{
    ViewData["Title"] = "إجازاتي";
}

<h2>إجازاتي</h2>

<hr />

<a href="/Leaves/Create" class="btn btn-success mb-3">طلب إجازة جديد</a>
<a href="/Leaves" class="btn btn-info mb-3">كل الطلبات (للمدير)</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>تاريخ البدء</th>
            <th>تاريخ الانتهاء</th>
            <th>عدد الأيام</th>
            <th>نوع الإجازة</th>
            <th>السبب</th>
            <th>الحالة</th>
            <th>تاريخ الطلب</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.StartDate.ToString("yyyy-MM-dd")</td>
                <td>@item.EndDate.ToString("yyyy-MM-dd")</td>
                <td>@item.TotalDays</td>
                <td>@item.LeaveType</td>
                <td>@item.Reason</td>
                <td>
                    @if (item.Status == "قيد الانتظار")
                    {
                        <span class="badge bg-warning">@item.Status</span>
                    }
                    else if (item.Status == "موافقة")
                    {
                        <span class="badge bg-success">@item.Status</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">@item.Status</span>
                    }
                </td>
                <td>@item.RequestDate.ToString("yyyy-MM-dd")</td>
            </tr>
        }
    </tbody>
</table>